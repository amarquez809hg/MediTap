DEBUG=True
SECRET_KEY=changeme
DATABASE_URL=postgres://meditap_user:meditap_pass@db:5432/meditap
ALLOWED_HOSTS=*

# Staff sign-in (Tab14): uncomment and paste Keycloak → Clients → meditap-elevate → Credentials.
# This file overrides docker/backend.dev.env when both are loaded by compose. Then recreate backend.
# KEYCLOAK_ELEVATE_CLIENT_SECRET=