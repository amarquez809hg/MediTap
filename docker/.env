DEBUG=True
SECRET_KEY=changeme
DATABASE_URL=postgres://meditap_user:meditap_pass@db:5432/meditap
ALLOWED_HOSTS=*

# Staff sign-in (Tab14): Django users in group meditap-record-editor (or superusers). No extra secret here.
# This file overrides docker/backend.dev.env when both are loaded by compose.