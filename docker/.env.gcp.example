# Copy to docker/.env and adjust for your VM / public host.
# MEDITAP_PUBLIC_HOST=YOUR_EXTERNAL_IP
# MEDITAP_PUBLIC_SCHEME=https
# (required for HTTPS SPA on a public IP — see docs/deploy-gcp-vm.md)
# MEDITAP_CORS_EXTRA_ORIGINS=https://YOUR_EXTERNAL_IP:8100
