# Optional overlay: publish the Ionic dev server on the host (backend :8080 is in base compose).
#
#   cd docker
#   docker compose -f docker-compose.yml -f docker-compose.local-ports.yml up -d --build

services:
  frontend:
    ports:
      - "8100:8100"
