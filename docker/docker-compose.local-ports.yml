# Local / LAN dev: publish backend and SPA on the host.
#
#   cd docker
#   docker compose -f docker-compose.yml -f docker-compose.local-ports.yml up -d --build

services:
  backend:
    ports:
      - "8080:8000"
  frontend:
    ports:
      - "8100:8100"
